# backend-frontend
This application is a website developed using HTML and Bootstrap as the frontend framework. The backend is implemented in PHP, and the database is managed using MySQL through the XAMPP environment.
